# REI'SPETOS OS v2.7 — Base Organizada

Esta atualização preserva os módulos da v2.6 e reorganiza o frontend com segurança.

## Correções
- restaura a abertura da tela Estoque;
- reduz o `main.jsx` ao bootstrap do React;
- move o sistema atual para `App.jsx`;
- separa configurações compartilhadas em `core/config.js`;
- prepara as pastas `components`, `pages`, `modules`, `services` e `hooks`.

## Arquivos para enviar
Substitua a pasta `frontend/src` usando o conteúdo deste pacote.

Não altere o backend nesta atualização.
